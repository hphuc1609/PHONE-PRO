import { createTheme } from "@mui/material"

const theme = createTheme({
  palette: {
    primary: {
      main: "#3977ce",
      dark: "#2e3192",
    },
    secondary: {
      main: "#23A35F",
    },
  },
})

export default theme
